apiVersion: v1
kind: Service

metadata:
  name: {{ .Release.Name }}-mongodb-service

spec:
  clusterIP: None

  selector:
    app: mongodb

  ports:
    - port: 27017
      targetPort: 27017
